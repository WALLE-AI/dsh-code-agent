# DSH Code Agent 代码覆盖率分析报告

## 报告信息

- 测试日期：2026-08-21
- 测试框架：Vitest 3.2.7
- 覆盖率引擎：`@vitest/coverage-v8` 3.2.7
- 统计范围：`packages/dsh-tui/src/**/*.{ts,tsx}`
- 源文件统计：65 个 TypeScript/TSX 文件
- 覆盖率报告：`/tmp/dsh-code-agent-coverage/coverage-summary.json`

## 执行结果

### 完整测试

无覆盖率插桩的完整测试结果：

| 项目 | 结果 |
| --- | ---: |
| 测试文件 | 52/52 通过 |
| 测试用例 | 584/584 通过 |
| 执行结果 | 通过 |

完整测试包含 shell 补全验证和 transcript 性能阈值测试。

### 覆盖率测试

| 项目 | 已覆盖 | 总数 | 覆盖率 |
| --- | ---: | ---: | ---: |
| 语句 | 6,832 | 7,453 | 91.66% |
| 行 | 6,832 | 7,453 | 91.66% |
| 函数 | 492 | 530 | 92.83% |
| 分支 | 3,120 | 3,689 | 84.57% |

覆盖率模式下共执行 50 个测试文件、573 个测试用例，全部通过。

## 测试口径说明

覆盖率运行排除了以下文件：

- `packages/dsh-tui/tests/completions.spec.ts`：8 个测试依赖启动真实 Bash 子进程，在受限测试环境中会出现 `spawnSync bash EPERM`。这些测试已在完整测试模式下通过。
- `packages/dsh-tui/tests/transcript-performance.spec.ts`：3 个测试使用严格的耗时阈值。V8 覆盖率插桩会显著增加运行时间，不适合用于判断真实性能。这些测试已在无插桩模式下通过。

上述排除只影响覆盖率测试的执行用例，不会从源代码覆盖率统计范围中删除对应业务文件。

## 低覆盖率区域

按行覆盖率排序，当前最需要补充测试的文件如下：

| 文件 | 行覆盖率 | 分支覆盖率 | 函数覆盖率 | 主要风险 |
| --- | ---: | ---: | ---: | --- |
| `src/render-boundary.tsx` | 52.38% | 66.66% | 50.00% | 渲染异常和恢复路径覆盖不足 |
| `src/views/question.tsx` | 60.60% | 11.11% | 100.00% | 问题表单的条件渲染分支覆盖不足 |
| `src/plugin.ts` | 62.40% | 68.90% | 62.06% | 插件生命周期、错误处理及集成分支覆盖不足 |
| `src/input-dispatch.ts` | 67.58% | 65.09% | 83.33% | 输入路由和特殊按键组合存在未覆盖路径 |
| `src/tool-card.ts` | 85.98% | 75.25% | 94.44% | 部分工具类型和异常展示状态未覆盖 |
| `src/app.tsx` | 86.79% | 75.89% | 57.14% | 顶层应用生命周期及回调函数覆盖不足 |
| `src/harness-adapter.ts` | 90.39% | 72.85% | 95.00% | Harness 异常事件和少见状态转换覆盖不足 |

`src/contracts.ts` 在 V8 报告中显示为 0%，但该文件主要包含 TypeScript 类型声明。类型会在编译后被擦除，因此不应将它视为运行时代码覆盖缺口。

## 分析结论

当前代码的行覆盖率和函数覆盖率均超过 90%，核心状态管理、终端布局、输入编辑、会话浏览、滚动、主题和关闭流程已有较完整的测试保护。584 个完整测试全部通过，说明当前版本在既有测试范围内没有发现功能回归。

分支覆盖率为 84.57%，明显低于行覆盖率。主要缺口集中在错误边界、插件生命周期、顶层应用回调、问题表单条件渲染，以及输入分发的边缘路径。这些区域通常与异常恢复和用户交互边界相关，建议优先补充测试。

## 后续建议

1. 优先为 `render-boundary.tsx` 增加渲染异常、重试和 fallback 恢复测试。
2. 为 `views/question.tsx` 补充不同问题类型、空选项、取消及校验失败分支。
3. 为 `plugin.ts` 增加初始化失败、重复注册、关闭清理和 Harness 异常事件测试。
4. 为 `input-dispatch.ts` 增加组合键、未知输入、overlay 切换及不同应用状态下的路由测试。
5. 为 `app.tsx` 增加启动失败、恢复会话、取消、退出和顶层回调测试。
6. 在 CI 中增加稳定的覆盖率命令，并将初始阈值设为：行 90%、语句 90%、函数 90%、分支 80%。
7. 对性能测试继续使用无覆盖率插桩的独立任务，避免插桩开销造成误报。

## 建议验收标准

短期目标是在不降低现有覆盖率的基础上，将以下关键文件的行覆盖率提高到 80% 以上：

- `render-boundary.tsx`
- `views/question.tsx`
- `plugin.ts`
- `input-dispatch.ts`

中期目标是将项目整体分支覆盖率提高到 88%，并在 CI 中阻止覆盖率低于基线的新变更合入。
